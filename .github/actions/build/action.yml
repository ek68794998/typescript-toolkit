name: Build & test
description: "Builds all packages and runs tests"

runs:
  using: "composite"
  steps:
    - name: Build packages
      run: pnpm build
      shell: bash
    - name: Run tests
      run: pnpm test
      shell: bash
